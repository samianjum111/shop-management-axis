<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
    <title>Transcript - Order #{{ order.id }}</title>
    <style>
        /* Reset & Base */
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: #f8f9fa;
            padding: 16px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        .transcript-container {
            max-width: 480px;
            width: 100%;
            background: #ffffff;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
            padding: 24px 20px;
            position: relative;
            overflow: hidden;
            margin: 0 auto;
        }
        /* Watermark */
        .watermark {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-30deg);
            font-size: 3.5rem;
            font-weight: 800;
            color: rgba(0,0,0,0.04);
            pointer-events: none;
            white-space: nowrap;
            z-index: 0;
        }
        /* Header */
        .transcript-header {
            text-align: center;
            border-bottom: 2px solid #1a1a2e;
            padding-bottom: 12px;
            margin-bottom: 16px;
            position: relative;
            z-index: 1;
        }
        .transcript-header h1 {
            font-size: 1.4rem;
            font-weight: 700;
            color: #1a1a2e;
            margin: 0;
        }
        .transcript-header p {
            font-size: 0.85rem;
            color: #6c757d;
            margin: 2px 0 0;
        }
        /* Body */
        .transcript-body {
            position: relative;
            z-index: 1;
        }
        .transcript-body .order-title {
            font-size: 1.1rem;
            font-weight: 600;
            color: #1a1a2e;
            margin: 0 0 4px;
        }
        .customer-details {
            font-size: 0.9rem;
            color: #495057;
            margin-bottom: 12px;
            line-height: 1.4;
        }
        .customer-details strong {
            color: #1a1a2e;
        }
        /* Table */
        .transcript-body table {
            width: 100%;
            border-collapse: collapse;
            font-size: 0.8rem;
            margin-top: 4px;
        }
        .transcript-body table th,
        .transcript-body table td {
            padding: 6px 4px;
            border: 1px solid #dee2e6;
            text-align: left;
            vertical-align: top;
        }
        .transcript-body table th {
            background: #f8f9fa;
            font-weight: 600;
        }
        .text-right {
            text-align: right;
        }
        /* Totals */
        .totals {
            margin-top: 12px;
            font-size: 0.9rem;
            border-top: 1px solid #dee2e6;
            padding-top: 8px;
        }
        .totals .row {
            display: flex;
            justify-content: space-between;
            padding: 2px 0;
        }
        .totals .row.total {
            font-weight: 700;
            font-size: 1rem;
            border-top: 2px solid #1a1a2e;
            margin-top: 4px;
            padding-top: 4px;
        }
        .totals .row.remaining {
            color: #e67e22;
        }
        /* Footer */
        .transcript-footer {
            text-align: center;
            font-size: 0.75rem;
            color: #6c757d;
            border-top: 1px solid #dee2e6;
            padding-top: 10px;
            margin-top: 16px;
            position: relative;
            z-index: 1;
        }
        /* Print button */
        .print-actions {
            text-align: center;
            margin-top: 20px;
            display: flex;
            justify-content: center;
            gap: 8px;
            flex-wrap: wrap;
        }
        .print-actions button {
            background: #e67e22;
            color: white;
            border: none;
            border-radius: 30px;
            padding: 8px 20px;
            font-size: 0.85rem;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.2s;
            display: inline-flex;
            align-items: center;
            gap: 6px;
        }
        .print-actions button:hover {
            background: #d35400;
        }
        .print-actions button.secondary {
            background: #6c757d;
        }
        .print-actions button.secondary:hover {
            background: #5a6268;
        }

        /* Print Styles */
        @media print {
            body {
                background: white;
                padding: 0.4in;
                min-height: auto;
                display: block;
            }
            .transcript-container {
                box-shadow: none;
                border-radius: 0;
                padding: 0;
                max-width: 100%;
            }
            .watermark {
                font-size: 5rem;
                color: rgba(0,0,0,0.06);
            }
            .print-actions {
                display: none !important;
            }
            .transcript-body table {
                font-size: 0.7rem;
            }
            .transcript-body table th,
            .transcript-body table td {
                padding: 4px 3px;
            }
            .transcript-header h1 {
                font-size: 1.2rem;
            }
            .transcript-footer {
                font-size: 0.65rem;
            }
            .totals .row {
                padding: 1px 0;
            }
            /* Ensure table doesn't break across pages */
            table { page-break-inside: avoid; }
            .transcript-body { page-break-after: avoid; }
        }

        /* Small screen tweaks */
        @media (max-width: 400px) {
            .transcript-container { padding: 16px 12px; }
            .transcript-body table { font-size: 0.7rem; }
            .transcript-body table th,
            .transcript-body table td { padding: 4px 2px; }
            .transcript-header h1 { font-size: 1.2rem; }
            .watermark { font-size: 2.8rem; }
        }
    </style>
</head>
<body>
    <div class="transcript-container">
        <div class="watermark">
            {% if order.payment_status == 'paid' %}PAID{% elif order.payment_status == 'partial' %}PARTIAL{% else %}UNPAID{% endif %}
        </div>

        <div class="transcript-header">
            <h1>{{ tenant.name }}</h1>
            <p>Flour Mill Invoice</p>
        </div>

        <div class="transcript-body">
            <div class="order-title">Order #{{ order.id }}</div>
            <div class="customer-details">
                <strong>Customer:</strong> {{ order.customer.name }}<br>
                <strong>Phone:</strong> {{ order.customer.phone|default:"N/A" }}<br>
                <strong>Address:</strong> {{ order.customer.address|default:"N/A" }}
            </div>

            <table>
                <thead>
                    <tr>
                        <th>Category</th>
                        <th>KG</th>
                        <th>Clean</th>
                        <th class="text-right">Grind</th>
                        <th class="text-right">Clean Chg</th>
                        <th class="text-right">Total</th>
                    </tr>
                </thead>
                <tbody>
                    {% for item in order.items.all %}
                    <tr>
                        <td>{{ item.category.name }}</td>
                        <td>{{ item.total_kg }}</td>
                        <td>{{ item.is_cleaning_done|yesno:"Yes,No" }}</td>
                        <td class="text-right">₹{{ item.grinding_charges|floatformat:2 }}</td>
                        <td class="text-right">₹{{ item.cleaning_charges|floatformat:2 }}</td>
                        <td class="text-right">₹{{ item.item_total|floatformat:2 }}</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>

            <div class="totals">
                <div class="row"><span>Grand Total</span><span>₹{{ order.total_amount|floatformat:2 }}</span></div>
                <div class="row"><span>Amount Paid</span><span>₹{{ order.amount_paid|floatformat:2 }}</span></div>
                <div class="row remaining"><span>Remaining</span><span>₹{{ order.remaining_amount|floatformat:2 }}</span></div>
                <div class="row total"><span>Payment Status</span><span>{{ order.payment_status|title }}</span></div>
            </div>
        </div>

        <div class="transcript-footer">
            Generated on {% now "d M Y H:i" %} · Thank you!
        </div>
    </div>

    <div class="print-actions no-print" style="display:flex; flex-wrap:wrap; gap:10px; justify-content:center; margin-top:20px;">
        <button onclick="window.print()" style="background:#2c3e50; color:white; border:none; border-radius:30px; padding:10px 22px; font-weight:600; display:inline-flex; align-items:center; gap:8px; cursor:pointer; transition:0.2s;">
            <i class="fas fa-print"></i> Print
        </button>
        <div style="display:inline-flex; align-items:center; gap:6px; background:#f8f9fa; border-radius:30px; padding:2px 4px; border:1px solid #dee2e6;">
            <select id="downloadFormat" style="border:none; background:transparent; padding:8px 12px; font-weight:500; outline:none; border-radius:30px;">
                <option value="png">PNG</option>
                <option value="jpeg">JPEG</option>
                <option value="pdf">PDF</option>
            </select>
            <button id="downloadTranscriptBtn" style="background:#e67e22; color:white; border:none; border-radius:30px; padding:8px 20px; font-weight:600; display:inline-flex; align-items:center; gap:6px; cursor:pointer; transition:0.2s;">
                <i class="fas fa-download"></i> Download
            </button>
        </div>
    </div>

    <!-- Font Awesome for icons (if not already loaded) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

<script>
document.addEventListener('DOMContentLoaded', function() {
    const downloadBtn = document.getElementById('downloadTranscriptBtn');
    const formatSelect = document.getElementById('downloadFormat');
    if (downloadBtn) {
        downloadBtn.addEventListener('click', function() {
            const format = formatSelect.value;
            const container = document.querySelector('.transcript-container');
            if (!container) return;
            html2canvas(container, {
                scale: 2,
                useCORS: true,
                backgroundColor: '#ffffff'
            }).then(canvas => {
                if (format === 'pdf') {
                    const imgData = canvas.toDataURL('image/png');
                    const { jsPDF } = window.jspdf;
                    const pdf = new jsPDF('p', 'mm', 'a4');
                    const pdfWidth = pdf.internal.pageSize.getWidth();
                    const pdfHeight = (canvas.height * pdfWidth) / canvas.width;
                    pdf.addImage(imgData, 'PNG', 0, 0, pdfWidth, pdfHeight);
                    pdf.save('transcript.pdf');
                } else {
                    const mimeType = format === 'png' ? 'image/png' : 'image/jpeg';
                    const ext = format === 'png' ? 'png' : 'jpg';
                    const link = document.createElement('a');
                    link.download = `transcript.${ext}`;
                    link.href = canvas.toDataURL(mimeType);
                    link.click();
                }
            }).catch(err => {
                alert('Failed to generate download: ' + err.message);
            });
        });
    }
});
</script>

</body>
</html>
